#include "Scrabble.hpp"
#include "Action.hpp"

bool Scrabble::isMoveValid(Action action, const Board& board) const {
    return true;
}

bool Scrabble::checkVictory(const Board& board) const {
    return false;
}